<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.irsis">
    <!-- 声明权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/irsis"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/irsis"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NotificationActivity"></activity>
        <activity android:name=".SubmitProblemActivity" />
        <activity android:name=".LitePalActivity" />
        <activity android:name=".FilePersistenceActivity" />
        <activity android:name=".ProblemContentActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ChatActivity" /> <!-- 启动页面 -->
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 第三个活动，设置为能够响应打开网页的Intent,可以响应所有的http协议 -->
        <activity android:name=".RuntimePermissionActivity">
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="http" />
            </intent-filter>
        </activity> <!-- 第二个活动 -->
        <activity android:name=".ProblemActivity">
            <intent-filter>

                <!-- 指明当前活动可以响应com.example.activitytest.ACTION_START这个action -->
                <action android:name="com.example.activitytest.ACTION_START" />
                <!-- category标签包含了一些附加信息，更精确的指明了当前活动能够响应Intent中还可能带有的category -->
                <category android:name="android.intent.category.DEFAULT" />
                <!-- 自定义一个category -->
                <category android:name="com.example.activitytest.MY_CATEGORY" />
            </intent-filter>
        </activity> <!-- 主活动 -->
        <activity android:name=".MainActivity" />
    </application>

</manifest>